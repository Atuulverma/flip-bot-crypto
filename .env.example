# Delta API keys
DELTA_API_KEY=changeme
DELTA_API_SECRET=changeme

# Symbols
SYMBOLS=SOLUSD
RESOLUTIONS=5m

# Risk knobs (per symbol defaults)
LEVERAGE_DEFAULT=5
CAPITAL_ALLOC_PCT_PER_TRADE=0.05
MAX_DAILY_LOSS_PCT=0.02
CATASTROPHE_SL_ENABLE=false
CATASTROPHE_SL_ATR_MULT=2.5

# Supertrend presets (JSON)
SUPERTREND_PRESETS={"SOLUSD": {"period": 10, "multiplier": 3.0}}

# Context behavior
CONTEXT_VETO_STRICT=false
